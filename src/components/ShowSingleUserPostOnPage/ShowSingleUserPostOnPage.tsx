//import viewPost from "../../api/posts/viewPost";
//import { userToken } from "../../api/constants";
export default function ShowSingleUserPostOnPage() {
  /*
  const viewSinglePost = async function (id: string) {
    try {
      const SINGLE_POST_URL = `/${id}`;
      await viewPost(SINGLE_POST_URL, userToken);
    } catch (error) {
      console.log(error);
    }
  };
  */
  return (
    <>
      <h1>hello from other users post page</h1>
    </>
  );
}
